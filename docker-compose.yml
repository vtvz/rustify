version: "3.8"
services:
  app:
    build:
      context: .
    env_file:
      - .env.deploy
    restart: always
    volumes:
      - ./var:/var/app
    #  - ./proxychains.conf:/etc/proxychains4.conf
    # entrypoint: [proxychains]
    # command: [/usr/local/bin/rustify]
